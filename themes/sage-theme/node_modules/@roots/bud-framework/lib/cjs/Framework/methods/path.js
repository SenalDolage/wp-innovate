"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.path = void 0;
const path_1 = require("path");
const path = function (key, ...path) {
    const project = this.hooks.filter(`location.project`);
    const partial = this.hooks.filter(`location.${key}`);
    const useAbsolute = project === partial || partial.startsWith('/');
    return useAbsolute
        ? (0, path_1.join)(...[partial, ...(path ?? [])].filter(Boolean))
        : (0, path_1.join)(...[project, partial, ...(path ?? [])].filter(Boolean));
};
exports.path = path;
