import { Compiler as Contract, Service } from '@roots/bud-framework';
import { Configuration, MultiStats, StatsCompilation, StatsError } from 'webpack';
/**
 * Wepback compilation controller class
 *
 * @public
 */
export declare class Compiler extends Service implements Contract {
    /**
     * Compiler instance
     *
     * @public
     */
    instance: Contract.Instance;
    /**
     * Compilation stats
     *
     * @public
     */
    stats: StatsCompilation;
    /**
     * Compiler errors
     *
     * @public
     */
    errors: Array<StatsError>;
    /**
     * Compilation progress
     *
     * @public
     */
    progress: Contract.Progress;
    /**
     * Multi-compiler configuration
     *
     * @public
     */
    config: Array<Configuration>;
    /**
     * Initiates compilation
     *
     * @returns the compiler instance
     *
     * @public
     * @decorator `@bind`
     */
    compile(): Promise<import("webpack").MultiCompiler>;
    /**
     * @public
     * @decorator `@bind`
     */
    invoke(config: Array<Configuration>): Promise<import("webpack").MultiCompiler>;
    /**
     * Returns final webpack configuration
     *
     * @public
     * @decorator `@bind`
     */
    before(): Promise<Configuration[]>;
    /**
     * Webpack callback
     *
     * @public
     * @decorator `@bind`
     */
    callback(error: Error, stats: MultiStats): Promise<void>;
    /**
     * Stats handler
     *
     * @public
     * @decorator `@bind`
     */
    handleStats(stats: MultiStats): Promise<void>;
    /**
     * Error handler
     *
     * @public
     * @decorator `@bind`
     */
    handleErrors(error: Error): Promise<void>;
    /**
     * Progress callback
     *
     * @public
     * @decorator `@bind`
     */
    progressCallback(percent: number, scope: string, ...message: any[]): Promise<void>;
}
//# sourceMappingURL=compiler.service.d.ts.map