import { Dashboard as Contract } from '@roots/bud-framework';
import { Service } from '@roots/bud-framework';
/**
 * Dashboard service
 *
 * @public
 */
export declare class Dashboard extends Service implements Contract {
    /**
     * Boot
     *
     * @public
     * @decorator `@bind`
     * @decorator `@once`
     */
    boot(): Promise<void>;
    /**
     * Run dashboard
     *
     * @public
     * @decorator `@bind`
     * @decorator `@once`
     */
    run(): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map