/**
 * stdio component
 *
 * @remarks
 * This is wrapped the way it is so we don't explode
 * everything if we're in an environment that doesn't
 * support rawMode (like CI)
 *
 * @public
 */
export declare const Input: ({ app }: {
    app: any;
}) => void;
//# sourceMappingURL=input.component.d.ts.map