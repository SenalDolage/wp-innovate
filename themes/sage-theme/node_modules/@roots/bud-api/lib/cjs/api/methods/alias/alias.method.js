"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.alias = void 0;
const path_1 = require("path");
/**
 * Register shorthand for resolving modules using webpack aliases.
 *
 * @remarks
 * Useful for situations that may otherwise require brittle relative paths.
 *
 * @example
 * ```js
 * app.alias({
 *   '@scripts': app.path('src', 'scripts'),
 * })
 * ```
 *
 * @param alias - module redirections
 * @returns configuration instance
 *
 * @public
 */
const alias = function (alias) {
    const ctx = this;
    const merged = Object.entries(alias).reduce((a, [k, v]) => ({
        ...a,
        [k]: (0, path_1.resolve)(ctx.path('project'), v),
    }), {});
    ctx.hooks.async('build.resolve.alias', async (aliases) => {
        return { ...aliases, ...merged };
    });
    return ctx;
};
exports.alias = alias;
