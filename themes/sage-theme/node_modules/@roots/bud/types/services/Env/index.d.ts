import type { Env as Base } from '@roots/bud-framework';
import { Service } from '@roots/bud-framework';
/**
 * Env service
 *
 * @public
 */
export declare class Env extends Service<Record<string, any>> implements Base {
    /**
     * Service ident
     *
     * @internal
     */
    ident: string;
    /**
     * Path to .env file
     *
     * @public @readonly
     */
    get envPath(): string;
    /**
     * Bootstrap event callback
     *
     * @internal
     * @decorator `@bind`
     */
    bootstrap(): Promise<void>;
    /**
     * Retrieve parsed env object
     *
     * @public
     * @decorator `@bind`
     */
    getParsedEnv(): Record<string, any>;
    /**
     * Get entries from .env which include `APP_PUBLIC` in their name
     *
     * @public
     * @decorator `@bind`
     */
    getPublicEnv(): Record<string, any>;
}
//# sourceMappingURL=index.d.ts.map