/// <reference types="node" />
import { Framework } from '@roots/bud-framework';
import { Connection } from '@roots/bud-framework/src/Server/Connection';
import { Signale } from '@roots/bud-support';
import { IncomingMessage, Server as HttpServer } from 'http';
import { Server as HttpsServer } from 'https';
import { ServerResponse } from 'webpack-dev-middleware';
/**
 * HTTP Server
 *
 * @public
 */
export declare abstract class BaseServer<T> implements Connection {
    app: Framework;
    url: URL;
    abstract createServer: Connection['createServer'];
    /**
     * Server instance
     *
     * @public
     */
    instance: T & (HttpServer | HttpsServer);
    /**
     * Port number
     *
     * @public
     */
    port: number;
    /**
     * Logger
     *
     * @public
     */
    logger: Signale;
    /**
     * Constructor
     *
     * @param app - Framework
     */
    constructor(app: Framework, url: URL);
    /**
     * setup
     *
     * @public
     */
    setup(): Promise<void>;
    /**
     * Listen
     *
     * @public
     */
    listen(): Promise<void>;
    /**
     * Server listen event
     *
     * @public
     */
    onListening(): void;
    /**
     * Server request
     *
     * @public
     * @decorator `@bind`
     */
    onRequest(request: IncomingMessage, response: ServerResponse): Promise<ServerResponse>;
    /**
     * Server error
     *
     * @param error - error
     *
     * @public
     * @decorator `@bind`
     */
    onError(error: Error): void;
}
//# sourceMappingURL=server.base.d.ts.map